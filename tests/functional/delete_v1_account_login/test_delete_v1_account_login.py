def test_delete_v1_account_login(auth_account_helper):
    # login = prepare_user.login
    # password = prepare_user.password
    # email = prepare_user.email
    # auth_account_helper.register_new_user(
    #     login=login,
    #     email=email,
    #     password=password
    # )

    #auth_account_helper.dm_account_api.account_api.get_v1_account()
    auth_account_helper.logout_user()
    #auth_account_helper.user_login(login=login, password=password)
